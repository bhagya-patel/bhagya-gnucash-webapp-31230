apiVersion: v1
kind: Service
metadata:
  name: {{ include "gnucash-app.fullname" . }}
  labels:
    {{- include "gnucash-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.app.service.type }}
  ports:
    - port: {{ .Values.app.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gnucash-app.selectorLabels" . | nindent 4 }}
