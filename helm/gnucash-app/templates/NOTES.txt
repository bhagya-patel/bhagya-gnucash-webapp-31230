🚀 GnuCash App has been deployed!

{{- if .Values.ingress.enabled }}
Your application is available at:
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  https://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else if contains "NodePort" .Values.app.service.type }}
Get the application URL by running:
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "gnucash-app.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT
{{- else if contains "LoadBalancer" .Values.app.service.type }}
Get the application URL by running:
  NOTE: It may take a few minutes for the LoadBalancer IP to be available.
  kubectl get --namespace {{ .Release.Namespace }} svc -w {{ include "gnucash-app.fullname" . }}
{{- else if contains "ClusterIP" .Values.app.service.type }}
Get the application URL by running:
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "gnucash-app.fullname" . }} 8080:{{ .Values.app.service.port }}
  Then visit: http://localhost:8080
{{- end }}

📊 Deployment Details:
  - Release: {{ .Release.Name }}
  - Namespace: {{ .Release.Namespace }}
  - Environment: {{ .Values.global.environment }}
  - Replicas: {{ .Values.app.replicaCount }}
  - Image: {{ include "gnucash-app.image" . }}

🔧 Useful Commands:
  # Check pod status
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "gnucash-app.name" . }}

  # View logs
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "gnucash-app.name" . }} -f

  # Scale deployment
  kubectl scale deployment {{ include "gnucash-app.fullname" . }} -n {{ .Release.Namespace }} --replicas=5

  # Check HPA status
  kubectl get hpa {{ include "gnucash-app.fullname" . }} -n {{ .Release.Namespace }}

{{- if .Values.migration.enabled }}

📦 Database Migration:
  Migration job will run automatically before deployment.
  Check migration status:
  kubectl get jobs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=migration
{{- end }}
